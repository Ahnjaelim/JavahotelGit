				<!-- 문자열 데이터를 날짜형 데이터로 변환, 이때 패턴은 해당 문자열의 패턴을 적어준다 -->
				<div class="hidden">
					<fmt:parseDate var="startday" value="${mapList[i][keydate].re_startday}" pattern="yyyy-MM-dd" />
					<fmt:parseDate var="endday" value="${mapList[i][keydate].re_endday}" pattern="yyyy-MM-dd" />
					<fmt:formatDate value="${startday}" pattern="yyyyMMdd" />
					<fmt:formatDate value="${endday}" pattern="yyyyMMdd" />
					<fmt:parseNumber value="${startday.time / (1000*60*60*24)}" integerOnly="true" var="startdayVal" scope="request"/>
					<fmt:parseNumber value="${endday.time / (1000*60*60*24)}" integerOnly="true" var="enddayVal" scope="request"/>
					<c:set var="lodgingday" value="${enddayVal - startdayVal}" />
					<fmt:formatNumber var="day" minIntegerDigits="2" value="${day+1}" type="number"/>
					<c:set var="addkeydate" value="${curYear}-${curMon}-${day}" />
				</div>